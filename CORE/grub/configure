unset CPP
./configure --prefix=$PREFIX 		\
	    --build=$LMBX_BUILD		\
            --libdir=$PREFIX/lib     	\
            --bindir=$PREFIX/bin     	\
            --sbindir=$PREFIX/bin	\
	    --with-bootdir=/usr/boot    \
            --sysconfdir=$PREFIX/etc 	\
            --disable-efiemu        	\
	    --disable-werror    
