export LDFLAGS="-L/usr/pkgs/libogg/lib -logg -L/usr/pkgs/flac/lib -L/usr/pkgs/libvorbis/lib -lm"

./configure --prefix=$PREFIX 			\
            --enable-vcut    			\
            --with-ogg=/usr/pkgs/libogg 	\
            --with-vorbis=/usr/pkgs/libvorbis 	
